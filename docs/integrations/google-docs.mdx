---
title: Google Docs Integration
---

# Integrating Google Docs with OpenInt

Integrate Google Docs seamlessly into your application via OpenInt's secure, user-friendly connector. This guide walks you through every step, from setting up a Google Cloud OAuth2 client, through configuring the right scopes, to launching authentication flows in your app. 

> ⚡ **Current support:** As of this release, OpenInt supports the [`documents.currentonly`](https://developers.google.com/workspace/docs/api/auth#docs-scopes) scope for Google Docs. See details below on usage and roadmap for expanded scope support.

---

## Prerequisites

- Access to the [Google Cloud Console](https://console.cloud.google.com/) with project creation permissions
- OpenInt administrator access (sign up [here](https://app.openint.dev/dashboard/sign-up))
- Ability to configure OAuth consent and credentials

---

# 1. Register a Google Cloud Project & OAuth Client

<Steps>
  <Step title="Login to Google Cloud Console">
    Go to [Google Cloud Console](https://console.cloud.google.com/). Sign in with your work Google account.
  </Step>
  <Step title="Create or Select a Project">
    At the top-left, select your existing Google Cloud project or click <b>New Project</b> to create one for your integration.
  </Step>
  <Step title="Enable the Google Docs API">
    Navigate to <b>APIs & Services → Library</b>.<br />
    Search for <b>Google Docs API</b> and click <b>Enable</b>.
  </Step>
  <Step title="Configure the OAuth Consent Screen">
    Go to <b>APIs & Services → OAuth consent screen</b>.<br />
    Choose <b>External</b> (unless only internal GSuite users will connect).<br />
    Fill in app details (App name, email, logo if desired).<br />
    Add required scopes (see below), and add your email to test users if application is not published.<br />
    Save and continue. For most cases, <b>do not submit for verification yet</b>.
  </Step>
  <Step title="Create OAuth Credentials">
    Under <b>APIs & Services → Credentials</b>, click <b>+ CREATE CREDENTIALS</b> and choose <b>OAuth client ID</b>.<br />
    Choose <b>Web application</b>.<br />
    Set an appropriate name, then add your application’s redirect URI. For OpenInt, this is typically:  
    <code>https://app.openint.dev/oauth/callback</code>
    <br />
    Create and <b>copy your new Client ID and Client Secret</b> to a safe place.
  </Step>
</Steps>

> 💡 **Tip:** Ensure the redirect URI is entered exactly. A mismatch will cause an "redirect_uri_mismatch" error on authentication.

---

# 2. Google Docs API Scopes

The Google Docs API supports a variety of OAuth scopes. Each defines a different permission set for Docs/Drive files. For security and privacy, request the most restricted (least privilege) scope needed by your app.

MintlifyTable (no markdown tables, provide using Mintlify's UI components like Table and Tooltip):

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Cell>Scope</Table.Cell>
      <Table.Cell>Description</Table.Cell>
      <Table.Cell>Supported by OpenInt?</Table.Cell>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides access only to the currently open Google Docs document (when used with Docs add-ons). Most limited scope for temporary access.">https://www.googleapis.com/auth/documents.currentonly</Tooltip></Table.Cell>
      <Table.Cell>Current document read/write <br/>(most limited)</Table.Cell>
      <Table.Cell><strong>✅ Yes</strong></Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides full read/write access to Google Docs documents. Allows viewing, creating, editing, and deleting documents.">https://www.googleapis.com/auth/documents</Tooltip></Table.Cell>
      <Table.Cell>All Docs read/write</Table.Cell>
      <Table.Cell>🚫 No</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides read-only access to Google Docs documents. Allows viewing document content and metadata but no modifications.">https://www.googleapis.com/auth/documents.readonly</Tooltip></Table.Cell>
      <Table.Cell>All Docs read-only</Table.Cell>
      <Table.Cell>🚫 No</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides access to Google Docs files the app has created or is granted access to. Limited to specific files." >https://www.googleapis.com/auth/drive.file</Tooltip></Table.Cell>
      <Table.Cell>App-specific Drive files</Table.Cell>
      <Table.Cell>🚫 No</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides read-only access to all Google Drive files including Docs, but no modifications allowed. Wider access than drive.file but limited to viewing.">https://www.googleapis.com/auth/drive.readonly</Tooltip></Table.Cell>
      <Table.Cell>Drive files read-only</Table.Cell>
      <Table.Cell>🚫 No</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell><Tooltip text="Provides full read/write access to all Google Drive files including Docs. The broadest permission for Docs/Drive operations.">https://www.googleapis.com/auth/drive</Tooltip></Table.Cell>
      <Table.Cell>All Drive files full access</Table.Cell>
      <Table.Cell>🚫 No</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

> For the full, most up-to-date scope documentation, see the [Google Docs API OAuth scopes reference](https://developers.google.com/workspace/docs/api/auth#docs-scopes).

---

# 3. Configuring OpenInt with Your Credentials

Once you have your Google OAuth Client ID and Secret, plug them into the Connectors section of your OpenInt dashboard (see [Connector Configuration guide](/core-concepts-v0/connector-config)).

You will need to set:
- **Client ID**: from Google Cloud credentials
- **Client Secret**: from Google Cloud credentials
- **Redirect URI**: <code>https://app.openint.dev/oauth/callback</code>
- **Scope**: <code>https://www.googleapis.com/auth/documents.currentonly</code>

Example configuration:

```json
{
  "provider": "google-docs",
  "client_id": "YOUR_GOOGLE_CLIENT_ID",
  "client_secret": "YOUR_GOOGLE_CLIENT_SECRET",
  "scopes": ["https://www.googleapis.com/auth/documents.currentonly"],
  "redirect_uri": "https://app.openint.dev/oauth/callback"
}
```

---

# 4. Embedding the OpenInt Connect Widget

You can trigger the Google Docs OAuth flow in your application UI using the Connect Widget:

```jsx
import { ConnectEmbed } from '@openint/connect'

// Token generated server-side after user chooses Google Docs connection
declare const token: string

return <ConnectEmbed token={token} />
```

See the [Embed guide](/connect/embed) for details on generating the token and widget usage.

---

# Common Mistakes & Troubleshooting

- **Redirect URI mismatch:** Ensure the URI in your Google Cloud credentials exactly matches <code>https://app.openint.dev/oauth/callback</code>.
- **Consent screen not published/test user not added:** If using "Testing" mode, add your email as a test user.
- **Scope errors:** Only <code>documents.currentonly</code> is supported by OpenInt right now. Other scopes will silently fail or be rejected by Google/OpenInt.
- **Missing API enablement:** Double-check that the Google Docs API is enabled in your Cloud project.
- **User denied consent:** Users must grant permission for your requested scope.
- **Unverified app warnings:** If you use sensitive scopes or haven’t fully configured branding, Google may present the "unverified app" screen. For most integrations, do <u>not</u> request verification unless you’re ready for security review.

For more troubleshooting tips, see the [OpenInt FAQ](/connect/faq) or [Google's OAuth troubleshooting page](https://developers.google.com/identity/protocols/oauth2#troubleshooting).

---

# References & Further Reading

- [Google Docs API OAuth scope reference](https://developers.google.com/workspace/docs/api/auth#docs-scopes)
- [Configuring OAuth Consent Screen](https://developers.google.com/workspace/guides/configure-oauth-consent)
- [OpenInt Connector Configuration](/core-concepts-v0/connector-config)
- [OpenInt Embed Guide](/connect/embed)
- [Google Cloud Console](https://console.cloud.google.com/)
